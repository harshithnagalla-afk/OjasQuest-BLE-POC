{
  "name": "ble-pairing-poc",
  "private": true,
  "version": "0.1.0",
  "description": "Root convenience scripts — runs the backend and frontend dev servers together. Each half also has its own package.json and can be run independently from its own folder.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
